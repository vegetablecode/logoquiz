import { RiseLoader } from 'react-spinners';

const Spinner = () => (
  <div className="flex flex-row justify-center">
    <RiseLoader color="#f97316" size={10} />
  </div>
);

export default Spinner;
